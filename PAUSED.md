# SESSION PAUSED

**Where we stopped:**
We successfully implemented the `GET` (getAllProjects) and `POST` (createProject) methods. The `POST` method correctly writes data to our in-memory `ProjectService`. We successfully tested it using Postman.

We paused the session exactly as I introduced the concept of **Path Variables** and the `@DeleteMapping`. 

**Next step upon return:**
The user needs to write the `deleteProject` method in the Service and the `@DeleteMapping` in the Controller to finish Phase 2.

*Note: For full learning history, weak areas, and architecture context, please read `PROJECT_CONTEXT.md`.*
